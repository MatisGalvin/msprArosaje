import { StyleSheet } from "react-native";

const utilsStylesheet = StyleSheet.create({
    container: {
        flex: 1,
        padding: 16,
        paddingVertical: 0,
    },
    containerPadding: {
        paddingHorizontal: 16,
    }
});

export default utilsStylesheet;